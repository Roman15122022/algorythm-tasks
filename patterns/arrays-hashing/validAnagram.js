// Return true when both strings contain the same characters with the same
// frequencies. Character order does not matter.
function validAnagram(first, second) {
  // TODO: solve.
}

module.exports = validAnagram;
